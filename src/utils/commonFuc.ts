import AppleGameLottie from '@assets/lottie/apple-game.json';

import { LottieOptionTypes } from './types/common.type';

export const lottieOptions: LottieOptionTypes = {
  animationData: AppleGameLottie,
  autoplay: true,
  loop: true,
};
