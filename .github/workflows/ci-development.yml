name: Deploy Development CI
on:
  push:
    branches: [develop]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Set Environment Variables
        run: |
          echo "VITE_API_URL=$VITE_DEV_API_URL" >> .env
          echo "VITE_NODE_ENV=development" >> .env
        env:
          VITE_DEV_API_URL: ${{ secrets.VITE_DEV_API_URL }}

      - name: Build application
        run: pnpm build

      - name: Deploy to s3
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          aws s3 cp \
            --recursive \
            --region ap-northeast-2 \
            dist s3://dev.snackga.me

# TODO: Cloudfront 무효화 추가, 환경변수 등록 간소화, cp -> sync 명령어 대체 검토
